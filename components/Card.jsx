
const StrikingStatsCards = ({ channelInfo }) => {
  const formatNumber = (num) => {
    return new Intl.NumberFormat('en-US', { notation: 'compact', compactDisplay: 'short' }).format(num);
  };

  return (
    
  );
};

export default StrikingStatsCards;